please define OPENCV_ROOT when calling ndk-build

ndk-build OPENCV_ROOT=../../opencv

assuming ndk-build is in your path, and you call it from the
directory of this README.txt file, and ../../opencv is where
the opencv root is, that you have previously built with the
ndk.